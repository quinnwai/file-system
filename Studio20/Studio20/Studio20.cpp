// Studio20.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#pragma once
#include "../../SharedCode/TextFile.h"
#include "../../SharedCode/ImageFile.h"
#include "../../SharedCode/PasswordProxy.h"
#include <iostream>

using namespace std;

int main(int argc, char* argv[])
{
	/**
	string protection;
	cout << "what will your password be?" << endl;
	cin >> protection;
	AbstractFile* text = new TextFile("tfile.txt");
	PasswordProxy* protectFile = new PasswordProxy(text, protection);
	protectFile->read();
	cout << protectFile->write({'h', 'i', ' ', 'h', 'e', 'l', 'l', 'o'}) << endl;
	protectFile->read();
	cout << protectFile->getName() << endl;
	cout << protectFile->getSize() << endl;
	cout << protectFile->append({ 'b', 'y', 'e' }) << endl;
	cout << protectFile->write({ 'y', 'o', 'u',' ','s', 'u', 'c', 'k' });
	**/

}

