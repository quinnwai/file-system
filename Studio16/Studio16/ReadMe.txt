
1. Will LeVan, Quinn Wai Wong

2.	a. Declaring an interface requires creating a class and declaring public functions that can be used when the object is called
	
3	a. interface inheritance, as there is only a header file detailing the interface of the AbstractFile base class

4. We added print statements and called on each function to ensure that the functionr and the output was correct

5.
Main:
	af->getName();
	af->write({ 'w', 'r', 'i', 't', 'e' });
	af->append({ 'n', 'e', 'w' });
	af->read();
	af->getSize();

Output:
textFile constructor
FileName: str
write
vector swapped
new
writenew
size: 8